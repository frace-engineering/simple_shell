#include "shell.h"

/**
 * print_error - prints error message
 * @core: pointer to core variables
 * @msg: the message to display
 *
 * Description: prints error message to stderr when a program fails
 * Return: void
 */
void print_error(struct core *core, char *msg)
{
	(void) core;
	(void) msg;
}
